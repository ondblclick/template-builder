<% if (items.length <= 8) { %>
  <%- include('/partials/mixedtype/' + items.length + '.ejs', { items: items }) %>
<% } else { %>
  <%- include('/partials/mixedtype/9+.ejs', { items: items }) %>
<% } %>
